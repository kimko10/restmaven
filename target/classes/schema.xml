<?xml version="1.0" encoding="UTF-8"?>
<sql>
CREATE TABLE BOOK (
	ID BIGINT NOT NULL,
	TITLE VARCHAR(255),
	CREATOR VARCHAR(255),
	TYPE VARCHAR(255),
	DATE DATETIME,
	PRIMARY KEY (ID)
);
</sql>